<div class="taglinks">
  <span class="taglinks__label">{{ tags_label }}</span>
  <span class="taglinks__tags">
    {% for tag in tags %}
      <a class="taglinks__tags__link" href="{{ tag.url }}">{{ tag.name }}</a>{{ not loop.last ? ", " }}
    {% endfor %}
  </span>
</div>