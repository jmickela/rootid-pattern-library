{# 
  Required:

  logo - url to a logo image
  alt - alt text is required!

  Optional:

  logo_url - if you want the logo to link to an address other than current site root
#}
{% if logo_src and logo_alt %}
  <div class="sitelogo">
    <a class="sitelogo__link" href='{{ logo_url ? logo_url : "/" }}'>
      <img class='sitelogo__logo' src='{{ logo_src }}' alt='{{ logo_alt }}' />
    </a>
  </div>
{% endif %}